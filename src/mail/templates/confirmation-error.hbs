<!DOCTYPE html>
<html>
<head>
    <title>Error de Confirmación - Hoken</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9fafb;
            color: #2A3E4C;
            text-align: center;
            padding: 50px 20px;
        }
        .error {
            color: #f44336;
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        p {
            font-size: 18px;
            margin-bottom: 20px;
        }
        a.button {
            background-color: #2A3E4C;
            color: #00AEEF;
            padding: 14px 28px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: bold;
            font-size: 16px;
            display: inline-block;
            transition: background-color 0.3s ease;
        }
        a.button:hover {
            background-color: #1f2c36;
        }
    </style>
</head>
<body>
    <div class="error">Error al confirmar la cuenta</div>
    <p>{{error}}</p>
    <p>Por favor solicita un nuevo enlace de confirmación.</p>
    <a href="{{backendUrl}}/resend-confirmation" class="button" target="_blank" rel="noopener noreferrer">Reenviar correo de confirmación</a>
</body>
</html>
{{! Este es para el error de confirmación de cuenta. }}