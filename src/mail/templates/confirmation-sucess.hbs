<!DOCTYPE html>
<html>
<head>
    <title>Cuenta Confirmada</title>
    <style>
        body { font-family: Arial; text-align: center; padding: 40px; }
        .success { color: #4CAF50; font-size: 24px; }
        .token { 
            background: #f5f5f5; 
            padding: 15px; 
            margin: 20px auto; 
            max-width: 500px;
            word-break: break-all;
        }
    </style>
</head>
<body>
    <div class="success"> ¡Cuenta confirmada!</div>
    <p>Tu token de acceso es:</p>
    <div class="token">{{token}}</div>
    <p>
        <a href="{{frontendUrl}}/login?token={{token}}">
            Ir al login automáticamente
        </a>
    </p>
</body>
</html>